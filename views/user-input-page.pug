extends layout.pug 

block content 
    h1 User Input Page
    h3 You may customize your parameters to get best recommendations for your music taste.
    h4 If you do not specify optional params, they will be averaged from your top 20 tracks.
    form(method="post" action="/recommendations-for-user")
        p Playlist Name REQUIRED
        input(type='text', name='playlist_name', placeholder='Recommended Playlist')
        br
        p Seed Genres REQUIRED
        select(name='seed_genres')
            each val in Genres
                option=val
        br
        p Limit OPTIONAL
        input(type='text', name='limit', placeholder="10")
        br
        p Target Tempo OPTIONAL
        input(type="radio", id="slow",name='target_tempo', value=60)
        label(for="slow") Slow
        input(type="radio", id="fast", name='target_tempo', value=140)
        label(for="fast") Fast
        input(type='text', name='target_tempo', placeholder="120")
        br
        p Target Danceability OPTIONAL
        input(type="radio", id="low_danceable", name='target_danceability', value=0.3) 
        label(for="low_danceable") Low
        input(type="radio", id="high_danceable",name='target_danceability', value=0.8) 
        label(for="high_danceable") High
        input(type='text', name='target_danceability', placeholder="0.5")
        br
        p Target Energy OPTIONAL
        input(type='text', name='target_energy', placeholder="0.3")
        br
        p Target Acousticness OPTIONAL
        input(type='text', name='target_acousticness', placeholder="0.3")
        br
        input(type='submit', value='get recommendations')


